<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.group15project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group15Project"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestLoginActivity"/>
        <activity android:name=".RegistrationActivity"
            android:parentActivityName=".LoginActivity"/>
        <activity android:name=".TestRegistrationActivity"/>
        <activity android:name=".HomeActivity"/>
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".PostActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".ViewPostActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".PostActivity"
            android:parentActivityName=".HomeActivity"
            tools:ignore="DuplicateActivity" />
        <activity android:name=".HistoryActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".HistViewPostActivity"
            android:parentActivityName=".HistoryActivity"/>
        <activity android:name=".EditPostActivity"/>
        <activity android:name=".TestPostActivity"/>
        <activity android:name=".AcceptanceActivity"></activity>
    </application>

</manifest>
